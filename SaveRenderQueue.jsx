SL_NAME = "__settings__";(function main() {    queues = app.project.renderQueue.items;    for ( var i = 1; i <= items.length; i++ ) {        saveRenderSettings( queues[i] );    }})();function saveRenderSettings( queue ) {        var rs = {};                                // set value        var comp = queue.comp;    var sl = comp.layer( SL_NAME ); // 's'ettings 'l'ayer        if ( sl === null ) {        sl = comp.layers.addText("test");                $.writeln( sl );                sl.name = SL_NAME;        sl.shy = true;        sl.enabled = false;        sl.guideLayer = true;        sl.inPoint = 0.0;        sl.outPoint = comp.frameDuration;        sl.moveToBeginning();    }    sl.text.sourceText.setValue( "UncoUncoUncoUnco" );}