#include "../(bk_include)/common.jsx"run(function() {	var comp = getActiveComp();	var layers = getSelectedLayers();	var res =		"""			palette {				orientation: 'column', alignment: ['fill', 'top'],				name: Group {					orientation: 'row',					alignment: ['left', 'fill'],					label: StaticText {text: 'New composition name:'},					text: EditText {},				}			}		""";		// var comp = getActiveComp();	// var layers = getSelectedLayers(); //    var label = layers[0].label;	// indicies = [];	// for ( var i = 0; i < layers.length; i++ ) {	// 	indicies.push( layers[i].index );	// }	// var params = {	// 	name: { type: "string", value: "pre" }	// }	// var dialog = new Dialog( params );	// var result = dialog.show();	// if ( result !== null ) {	// 	// if ( result.trim ) {	// 	// 	for ( var i = 0; i < indicies.length; i++ ) {	// 	// 		var layer = comp.layer( indicies[i] ).inPoint;	// 	// 	}	// 	// }	// 	var precomp = comp.layers.precompose( indicies, comp.name + "_" + result.name, true );	// 	precomp.label = label;         //        // NOTE: //        var l = comp.layers[ indicies[0] ];         //        comp.layers[ indicies[0] ].label = label;       	// }});