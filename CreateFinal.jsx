#include "bk_include/util.jsx"var settings;var finalDir, timestamp;(function main() {        if ( (settings = loadSettings()) === null ) {        return;    }    finalDir = resolveRelativePath(        settings["project_path"],        settings["final"]["directory"]    );            timestamp = (function() {        var now = new Date();        return ( "0" + (now.getMonth()+1) ).substr( -2 ) +            ( "0" + now.getDate() ).substr( -2 );    })();    // get active compositions    items = getSelectedComp();        if ( items === null ) {        alert("No Composition Selected!");        return;    }    // create directory    Folder( finalDir + "/" + timestamp ).verify();        // add queue    for ( var i = 0; i < items.length; i++ ) {        addQueue( items[i] );    }        return;})();function addQueue( item ) {    // setup queue    var queue = app.project.renderQueue.items.add( item );    queue.applyTemplate( settings["final"]["render_settings"] );        var output = queue.outputModule( 1 );        output.applyTemplate( settings["final"]["output_module"] );        var destPath = finalDir + "/" + timestamp + "/"        + item.name + "_" + timestamp + ".mov";    output.file = new File( destPath );}